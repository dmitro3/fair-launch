{
  "name": "fair-launch-backend",
  "version": "1.0.0",
  "description": "Backend for Fair Launch token platform",
  "main": "index.ts",
  "scripts": {
    "dev": "bun run --watch index.ts",
    "start": "bun run index.ts",
    "test": "bun test src/tests/*.test.ts",
    "test:watch": "bun test --watch src/tests/*.test.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@hono/node-server": "^1.8.2",
    "@hono/zod-validator": "^0.2.1",
    "cors": "^2.8.5",
    "drizzle-orm": "^0.44.2",
    "hono": "^4.0.5",
    "postgres": "^3.4.4",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "@types/cors": "^2.8.17",
    "drizzle-kit": "^0.31.4",
    "typescript": "^5.3.3"
  }
}
